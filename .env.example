# Server
PORT=3000
HOST=localhost
# In production, set HOST=0.0.0.0 (or leave unset; API defaults to 0.0.0.0 when NODE_ENV=production).

# CORS: allowed origin for the frontend (e.g. https://your-app.vercel.app).
# Leave unset in development to use http://localhost:5173.
# CORS_ORIGIN=https://your-app.vercel.app

# Database: use PostgreSQL (e.g. Railway) when set; otherwise SQLite.
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# Or use Railway's variable: DATABASE_PUBLIC_URL (app checks both)

# SQLite (when DATABASE_URL is not set): path to SQLite file (optional; default: ./data/research.db).
# DATABASE_PATH=./data/research.db
# DB_PATH=./data/research.db
